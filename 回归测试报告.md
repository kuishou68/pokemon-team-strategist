# 回归测试报告（题目 B · 宝可梦队伍策略师）

> 日期：2026-06-26　环境：Node v22.22.0 / npm 11.12.0 / macOS (darwin 24.6.0)

## 1. 依赖安装

- `npm install`（workspaces：root + server + web）：**成功**，无致命错误（仅个别 deprecated 警告，不影响功能）。

## 2. 构建（tsc + vite build）

| 目标 | 命令 | 结果 |
|---|---|---|
| 后端 | `tsc -p server/tsconfig.json` | ✅ 通过，无 TS 错误 |
| 前端 | `tsc -b && vite build` | ✅ 通过，无 TS 错误、无 CSS 警告 |

前端产物：
```
dist/index.html                0.74 kB │ gzip 0.45 kB
dist/assets/index-*.css       16.27 kB │ gzip 4.21 kB
dist/assets/index-*.js       173.34 kB │ gzip 55.65 kB
```
（未引入重型图表库；种族值雷达图为自绘 SVG。）

## 3. 单元测试 / 集成测试（`npm test`，不依赖真实 key）

### 后端（server）：**67 passed / 67**，5 个文件全绿

| 文件 | 用例数 | 覆盖 |
|---|---|---|
| typechart.test.ts | 28 | 18×18 维度、倍率合法性、标志性 matchup、0× 免疫(7 例)、双属性叠加(相乘/归零) |
| analysis.test.ts | 10 | analyzeTeam 防御/进攻/短板识别；recommendSwitch 克制高分/速度扣分/免疫场景/权重和=1/诚实标注 |
| normalize.test.ts | 8 | zh-hans 提取 + fallback、图鉴清洗、种族值映射、双属性 slot 排序 |
| updateTeam.test.ts | 16 | 按 action 分支校验、中文名归一、add/remove/replace、满6拒绝、空、重复、幻觉宝可梦拒绝、按槽位/中文名定位 |
| agent.test.ts | 5 | 桩 LLM + 内存 emit sink：纯文本流式、update_team→team 事件时序、多轮 replace、stopWhen≤6、注入块不写入 messages |

### 前端（web）：**11 passed / 11**，2 个文件全绿

| 文件 | 用例数 | 覆盖 |
|---|---|---|
| api/chat.test.ts | 3 | SSE 跨 chunk 边界解析、忽略心跳、team 事件、非 2xx → error |
| components/components.test.tsx | 8 | PokemonCard 中英双名/双属性/总和/代理 url、StatRadar SVG+aria、TeamView 空态6槽/填充+移除回调、TypeCoverage 短板高亮/空态、EvolutionChain 分支渲染+条件 |

**合计：78 passed / 78。**

## 4. 后端实跑冒烟（构建产物，dummy key）

启动 `node dist/server/src/index.js`（PORT=8799），实测：

| 端点 | 结果 |
|---|---|
| `GET /api/health` | ✅ `{ok:true, model:...}` |
| `GET /api/pokemon/charizard` | ✅ 喷火龙 / 火,飞行 / total 534 / sprite=/api/sprite/6（**官方中文实测通过**） |
| `GET /api/evolution/eevee` | ✅ isBranching=true，root=伊布，8 分支（水/雷/火/太阳/月亮/叶/冰/仙子伊布）（**分支树实测通过**） |
| `GET /api/team`（空） | ✅ `{pokemon:[]}` |
| `GET /api/sprite/6` | ✅ 200 image/png 144568 字节（**代理实测通过**） |
| `GET /api/sprite/6`（二次） | ✅ time=0.0008s（**缓存命中实测通过**，对比首次网络拉取） |
| `GET /api/sprite/..%2f..`（非数字） | ✅ 400 invalid sprite id（**SSRF 收口实测通过**） |
| `POST /api/chat`（空 message） | ✅ 400「缺少 message 字段」 |
| `POST /api/chat`（有 message，dummy key） | ✅ 返回 SSE 头 + Set-Cookie（writeHead 前写出）+ `error` 事件优雅降级（**agent↔SSE 解耦 + cookie 时序 + 错误处理实测通过**） |

数据服务层另以 tsx 直跑验证：getPokemon / getMove(十万伏特/电/90/特殊) / getEvolutionChain 官方中文与进化条件中文全部正确。

## 5. 未验证项（需真实 key 的 E2E）

以下需填入真实 `OPENROUTER_API_KEY` 后由人工/浏览器验证（已在 `测试用例.md` 标注 ⓚ，且 Agent 循环已用桩 LLM 集成测试覆盖逻辑正确性）：

- LLM 实际工具选择与多轮编排（A1-A3、B1-B2、C1-C5、D1、E1、F1-F2、G1、G6、G8）。
- 浏览器端真实渲染与动画（H1-H6 中依赖实时对话的部分）。
- 流式 token 在真实模型下的逐字体验。

**纯数据/UI/SSRF/缓存/校验路径已实跑验证，无需 key。**

## 6. 音频

- `web/public/audio/bgm.mp3` **未包含**（CC0 音频二进制需用户自行下载放置）。
- 已提供：`AudioToggle` 组件 + `useAudio` hook（含 localStorage 偏好 + 文件缺失静默失败）+ `web/public/audio/README.md` 占位说明与 CC0 音源站点。
- 缺音频文件**不阻塞**任何其他功能。

## 7. 填入 key 后如何运行验证

```bash
cp .env.example .env          # 编辑 .env 填入 OPENROUTER_API_KEY
npm install                   # 若尚未安装
npm run dev                   # 后端 :8788 + 前端 :5173（Vite 代理 /api）
# 浏览器打开 http://localhost:5173，按 测试用例.md 跑 ⓚ 用例
```

回归（任意时刻）：
```bash
npm run build && npm test     # 当前：build 通过；78/78 测试通过
```

## 8. 结论

- 构建：✅ 后端 + 前端均通过，零 TS 错误、零 CSS 警告。
- 测试：✅ 78/78 全绿（核心正确性逻辑 typechart/评分/状态机/agent 全覆盖）。
- 冒烟：✅ 数据端点、官方中文、分支进化、图片代理 + 缓存 + SSRF、SSE/cookie 时序、错误降级实跑通过。
- 待人工：仅需真实 key 的 LLM E2E 与浏览器视觉验证；音频文件需用户提供。

**纵向切片完整可跑**：搜宝可梦 → 出结构化卡片 → 组队（服务端权威 + team 事件）→ 克制分析 / 对战建议 / 进化链，全链路就绪。
